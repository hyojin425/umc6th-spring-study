spring:
  datasource:
    url: jdbc:mysql://umc-6th-study.cshzc9vullbx.ap-northeast-2.rds.amazonaws.com/study
    username: root
    password: gywls1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: create
        default_batch_fetch_size: 1000